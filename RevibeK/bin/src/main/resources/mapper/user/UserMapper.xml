<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.revibek.user.mapper.UserMapper">

	<insert id="insertLocalUser">
		INSERT INTO users (nickname, email, password_hash,
		provider)
		VALUES (#{nickname}, #{email}, #{passwordHash}, 'local')
	</insert>

	<insert id="insertGoogleUser">
		INSERT INTO users (nickname, email, provider, provider_id)
		VALUES (#{nickname}, #{email}, 'google', #{providerId})
	</insert>

	<select id="selectUserById" parameterType="string"
		resultType="com.ssafy.revibek.user.dto.UserResponseDto">
		SELECT id, nickname, email, provider
		FROM users
		WHERE id =
		#{id}
	</select>

	<select id="selectUserAuthByEmail" parameterType="string"
		resultType="com.ssafy.revibek.user.dto.UserAuthDto">
		SELECT id, nickname, email, provider, provider_id, password_hash
		FROM users
		WHERE email = #{email}
	</select>

	<update id="updateUser">
		UPDATE users
		SET nickname = #{nickname}, email = #{email}
		WHERE id = #{id}
	</update>

	<delete id="deleteUser" parameterType="string">
		DELETE FROM users
		WHERE id
		= #{id}
	</delete>

</mapper>