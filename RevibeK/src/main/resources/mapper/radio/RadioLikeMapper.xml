<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.revibek.radio.mapper.RadioLikeMapper">

    <insert id="insertRadioLike">
        INSERT IGNORE INTO radio_likes (radio_session_id, user_id)
        VALUES (#{radioSessionId}, #{userId})
    </insert>

    <delete id="deleteRadioLike">
        DELETE FROM radio_likes
        WHERE radio_session_id = #{radioSessionId}
          AND user_id = #{userId}
    </delete>

    <select id="existsRadioLike" resultType="int">
        SELECT COUNT(1)
        FROM radio_likes
        WHERE radio_session_id = #{radioSessionId}
          AND user_id = #{userId}
    </select>

    <select id="countRadioLikes" resultType="int">
        SELECT COUNT(1)
        FROM radio_likes
        WHERE radio_session_id = #{radioSessionId}
    </select>

</mapper>
